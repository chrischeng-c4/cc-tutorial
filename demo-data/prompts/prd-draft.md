你正在協助 Case 04：PRD 初稿產生與審查。

請只讀這兩個檔案：
- `demo-data/fixtures/prd-template.md`
- `demo-data/fixtures/feature-brief-order-export.md`

任務：
依照 template，把 feature brief 整理成一份 PRD 草稿。

輸出規則：
- 用台灣繁體中文與台灣慣用詞輸出，禁止使用簡體中文與中國用語。
- 用 `目前` 不用 `當前`；用 `清單` 不用 `列表`；用 `email` 不用 `郵箱`；用 `依權限篩選` 不用 `按權限過濾`。
- 不要改檔，只在對話裡輸出結果。
- facts、assumptions、open questions 要分清楚。
- 不要替 Compliance / Finance / Engineering 做最終決策。
- 未確認的事項放在 `Open Questions`。
- 最後加一段 `Review Checklist`，列出上線前需要誰確認什麼。

輸出格式：
1. Problem
2. Goals
3. Non-goals
4. User Stories
5. Requirements
6. Edge Cases
7. Open Questions
8. Launch / Rollout
9. Review Checklist

完成後，請再補一段 `HITL Questions`，列出 PM、Tech Lead、Compliance、Support 各自要確認的問題。
